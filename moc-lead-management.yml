spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: myuser3
    password: mypassword3
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
