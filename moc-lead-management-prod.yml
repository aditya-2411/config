spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: myuser4
    password: mypassword4
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
